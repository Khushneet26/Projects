const fs = require("fs") 
const calculation = n *n ; {
    fs.writeFile(file, calculation.toString(), (err)) {
        if (err) return console.log(err);
        createBrotliCompress(null);
        console.log("I am inside the function !");
    }
}